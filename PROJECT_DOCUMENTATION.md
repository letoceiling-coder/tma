# WOW Spin - Полная документация проекта

> **Версия:** 2.1  
> **Последнее обновление:** Декабрь 2025  
> **Назначение:** Документация для понимания структуры, функционала и составления промптов для изменений

---

## 📋 Содержание

1. [Обзор проекта](#обзор-проекта)
2. [Архитектура](#архитектура)
3. [Структура проекта](#структура-проекта)
4. [Основные компоненты](#основные-компоненты)
5. [API Endpoints](#api-endpoints)
6. [База данных](#база-данных)
7. [Бизнес-логика](#бизнес-логика)
8. [Frontend структура](#frontend-структура)
9. [Интеграции](#интеграции)
10. [Правила разработки](#правила-разработки)

---

## 🎯 Обзор проекта

**WOW Spin** — Telegram Mini App с интерактивной рулеткой для розыгрыша призов.

### Технологический стек

**Backend:**
- Laravel 11 (PHP 8.2+)
- MySQL/PostgreSQL
- Redis (опционально, для кеширования)

**Frontend (Пользовательское приложение):**
- React 18 + TypeScript
- Vite (сборка)
- React Router DOM (роутинг)
- Tailwind CSS (стили)
- Shadcn/ui (UI компоненты)
- Sonner (уведомления)
- TanStack Query (управление состоянием)

**Frontend (Админ-панель):**
- Vue 3 + TypeScript
- Vite (сборка)
- Vue Router (роутинг)
- Tailwind CSS (стили)

**Интеграции:**
- Telegram Bot API
- Telegram Mini App API
- Telegram Stars API (платежи)
- VK Bot API (опционально)
- Max Bot API (опционально)

---

## 🏗 Архитектура

### Общая архитектура

```
┌─────────────────────────────────────────────────────────┐
│                    Telegram Mini App                      │
│              (React + TypeScript Frontend)                │
└──────────────────────┬──────────────────────────────────┘
                       │ HTTPS (initData)
                       ▼
┌─────────────────────────────────────────────────────────┐
│              Laravel Backend API                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ Controllers  │  │   Services   │  │    Models    │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└──────────────────────┬──────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────┐
│                    MySQL/PostgreSQL                      │
└─────────────────────────────────────────────────────────┘
```

### Поток данных

1. **Пользователь открывает Mini App** → Telegram передает `initData`
2. **Frontend валидирует initData** → Отправляет на сервер
3. **Backend валидирует initData** → Создает/обновляет пользователя
4. **Frontend запрашивает данные** → API возвращает конфигурацию
5. **Пользователь взаимодействует** → API обрабатывает запросы
6. **Backend отправляет уведомления** → Telegram Bot API

---

## 📁 Структура проекта

### Backend (Laravel)

```
app/
├── Console/
│   └── Commands/              # Artisan команды
│       ├── RestoreTickets.php
│       ├── SendTicketReminders.php
│       └── ...
│
├── Http/
│   ├── Controllers/
│   │   ├── Api/
│   │   │   ├── Admin/         # Админ API контроллеры
│   │   │   │   ├── WheelController.php      # Управление рулеткой
│   │   │   │   ├── ChannelController.php    # Управление каналами
│   │   │   │   ├── WowUserController.php    # Управление пользователями
│   │   │   │   └── ...
│   │   │   ├── WheelController.php          # Публичный API рулетки
│   │   │   ├── TicketController.php         # Управление билетами
│   │   │   ├── ReferralController.php       # Реферальная система
│   │   │   ├── LeaderboardController.php    # Лидерборд
│   │   │   ├── StarExchangeController.php   # Обмен Stars
│   │   │   ├── StarsPaymentController.php  # Платежи Stars
│   │   │   ├── TelegramWebhookController.php # Webhook от Telegram
│   │   │   └── ...
│   │   ├── Middleware/         # Middleware
│   │   │   ├── ValidateTelegramInitData.php
│   │   │   └── ...
│   │   └── Requests/           # Form Requests (валидация)
│   │
│   └── Resources/              # API Resources
│
├── Models/                     # Eloquent модели
│   ├── User.php                # Пользователь
│   ├── WheelSector.php        # Сектор рулетки
│   ├── WheelSetting.php       # Настройки рулетки
│   ├── Spin.php                # История прокрутов
│   ├── Referral.php            # Реферальные связи
│   ├── UserTicket.php          # История билетов
│   ├── StarExchange.php        # Обмены Stars
│   ├── Channel.php             # Каналы для подписки
│   └── ...
│
├── Services/                   # Бизнес-логика
│   ├── TelegramService.php            # Работа с Telegram
│   ├── TelegramNotificationService.php # Уведомления
│   ├── SubscriptionService.php        # Проверка подписки
│   ├── SupportService.php             # Поддержка
│   └── AdminMenu.php                  # Меню админки
│
├── Telegram/                   # Telegram интеграция
│   ├── Bot.php                 # Bot API методы
│   ├── MiniApp.php             # Валидация Mini App
│   ├── Keyboard.php            # Создание клавиатур
│   └── ...
│
├── Jobs/                       # Queue Jobs
│   └── Telegram/               # Отложенные задачи Telegram
│
└── Providers/                  # Service Providers
    ├── TelegramServiceProvider.php
    └── ...

database/
├── migrations/                 # Миграции БД
└── seeders/                    # Seeders

routes/
├── api.php                     # API маршруты
└── web.php                     # Web маршруты

config/
├── telegram.php               # Конфигурация Telegram
├── services.php                # Конфигурация сервисов
└── ...
```

### Frontend (React - пользовательское приложение)

```
frontend/
├── src/
│   ├── pages/
│   │   ├── Index.tsx           # Главная страница (роутинг)
│   │   ├── MainWheel.tsx       # Страница рулетки (основная)
│   │   ├── Friends.tsx         # Реферальная система
│   │   ├── Leaderboard.tsx     # Лидерборд
│   │   └── HowToPlay.tsx       # Инструкция
│   │
│   ├── components/
│   │   ├── WheelComponent.tsx  # Компонент рулетки
│   │   ├── SpinResultPopup.tsx # Попап результата
│   │   ├── SecretGiftPopup.tsx # Попап обмена Stars
│   │   ├── ReferralPopup.tsx   # Попап реферальной ссылки
│   │   ├── NoTicketsBanner.tsx # Баннер "нет билетов"
│   │   ├── StartMenu.tsx      # Стартовое меню
│   │   ├── LoadingScreen.tsx  # Экран загрузки
│   │   ├── BottomNav.tsx       # Нижняя навигация
│   │   └── ui/                 # Shadcn/ui компоненты
│   │
│   ├── hooks/
│   │   ├── useTelegramWebApp.ts      # Хук Telegram WebApp
│   │   ├── useChannelSubscription.ts # Проверка подписок
│   │   └── useUserInit.ts            # Инициализация пользователя
│   │
│   ├── lib/
│   │   ├── haptic.ts           # Тактильная обратная связь
│   │   └── utils.ts            # Утилиты
│   │
│   └── App.tsx                 # Корневой компонент
│
├── public/                     # Статические файлы
└── package.json
```

### Frontend (Vue - админ-панель)

```
resources/js/
├── pages/
│   ├── admin/
│   │   ├── Dashboard.vue       # Главная страница
│   │   ├── Users.vue           # Пользователи
│   │   ├── Roles.vue           # Роли
│   │   ├── wow/
│   │   │   ├── Wheel.vue       # Редактор рулетки
│   │   │   ├── Channels.vue    # Управление каналами
│   │   │   ├── WowUsers.vue   # Пользователи WOW
│   │   │   ├── Wins.vue       # Выигрыши
│   │   │   ├── Leaderboard.vue # Лидерборд
│   │   │   └── ...
│   │   ├── Settings.vue        # Настройки
│   │   └── BotConfig.vue       # Настройка бота
│   │
│   └── auth/
│       └── Login.vue           # Авторизация
│
├── layouts/
│   └── AdminLayout.vue         # Layout админки
│
├── components/                  # Vue компоненты
│
└── admin.js                     # Точка входа
```

---

## 🔧 Основные компоненты

### 1. Система рулетки (Wheel System)

**Модели:**
- `WheelSector` - секторы рулетки (12 штук)
- `WheelSetting` - настройки рулетки
- `Spin` - история прокрутов
- `PrizeType` - типы призов

**Контроллеры:**
- `WheelController` (публичный) - получение конфигурации и прокрут
- `WheelController` (Admin) - управление секторами и настройками

**Логика:**
- Вероятностная система выпадения призов
- Настраиваемые проценты для каждого сектора
- Автоматическое логирование всех прокрутов
- Обработка различных типов призов

**Типы призов:**
- `money` - денежные призы (рубли)
- `ticket` - билеты для дополнительных прокрутов
- `secret_box` - секретный бокс (требует связи с админом)
- `sponsor_gift` - подарок от спонсора
- `gift` - обычный подарок
- `empty` - пустой сектор (проигрыш)

### 2. Система билетов (Ticket System)

**Модели:**
- `User` - поле `tickets_available`
- `UserTicket` - история начисления/списания билетов

**Контроллеры:**
- `TicketController` - получение билетов, проверка восстановления

**Логика:**
- **Ежедневное начисление:** каждые 24 часа (настройка `daily_tickets`)
- **Начисление новым пользователям:** при первом входе (настройка `default_daily_tickets`)
- **Восстановление по таймеру:** через интервал `ticket_restore_hours` после обнуления
- **Отслеживание:** поле `last_ticket_received_at` для ежедневных билетов
- **Таймер:** поле `tickets_depleted_at` для восстановления

**Настройки (WheelSetting):**
- `daily_tickets` - количество билетов ежедневно (default: 1)
- `default_daily_tickets` - количество для новых пользователей (default: 1)
- `ticket_restore_hours` - интервал восстановления (default: 3)
- `send_ticket_notification` - отправлять уведомления (default: true)

### 3. Реферальная система (Referral System)

**Модели:**
- `Referral` - связи между пользователями
- `User` - поля `invited_by`, `referralsAsInviter()`

**Контроллеры:**
- `ReferralController` - получение ссылки, статистика
- `TelegramWebhookController` - регистрация по реферальной ссылке

**Логика:**
- Каждый пользователь получает уникальную реферальную ссылку
- При регистрации по ссылке:
  - Создается запись в `referrals`
  - Пригласившему начисляется 1 билет
  - Отправляется уведомление пригласившему
- Статистика: количество приглашенных, билеты за приглашения

### 4. Лидерборд (Leaderboard)

**Модели:**
- `LeaderboardSnapshot` - снимки лидерборда
- `LeaderboardPrize` - призы за места

**Контроллеры:**
- `LeaderboardController` - получение лидерборда
- `LeaderboardAdminController` - управление призами

**Логика:**
- Топ пользователей по количеству приглашенных
- Периоды: день, неделя, месяц, все время
- Настраиваемые призы за места 1-10
- Автоматические снимки лидерборда

### 5. Telegram Stars (Платежи)

**Модели:**
- `StarExchange` - обмены Stars на билеты
- `StarPayment` - история платежей
- `PaymentError` - ошибки платежей

**Контроллеры:**
- `StarExchangeController` - обмен Stars
- `StarsPaymentController` - обработка платежей

**Логика:**
- Покупка билетов за Telegram Stars
- Настройка: `stars_per_ticket_purchase` (default: 50)
- Настройка: `stars_enabled` (включить/выключить)
- Обработка webhook от Telegram

### 6. Система уведомлений

**Сервис:**
- `TelegramNotificationService` - отправка уведомлений

**Методы:**
- `notifyNewTicket()` - новый билет начислен
- `notifyWin()` - выигрыш приза
- `notifyNewReferral()` - новый реферал
- `notifyReminder()` - напоминание о билетах
- `notifyFreeSpinAvailable()` - доступна бесплатная прокрутка

**Настройки:**
- `send_ticket_notification` - уведомления о билетах (default: true)

### 7. Система подписки на каналы

**Модели:**
- `Channel` - каналы для обязательной подписки
- `ChannelClick` - логирование кликов

**Контроллеры:**
- `SubscriptionController` - проверка подписки
- `ChannelController` (Admin) - управление каналами

**Логика:**
- Обязательная подписка на каналы перед игрой
- Проверка через Telegram Bot API
- Логирование кликов по каналам
- Настраиваемый приоритет каналов

### 8. Админ-панель

**Структура:**
- Vue 3 + TypeScript
- Роутинг через Vue Router
- API через axios

**Разделы:**
- **WOW Рулетка:**
  - Редактор рулетки (секторы, вероятности)
  - Управление каналами
  - Пользователи WOW
  - Выигрыши
  - Лидерборд
  - Призы лидерборда
  - Статистика
  - Победители
  - Приветствие / Баннер
- **Конфигурации:**
  - Бот (настройка webhook)
  - Telegram Stars (настройки платежей)
- **Управление:**
  - Пользователи
  - Роли
  - Медиа
  - Уведомления

---

## 🌐 API Endpoints

### Публичные (не требуют авторизации)

```
GET  /api/channels                    # Список активных каналов
GET  /api/check-subscription/{username} # Проверка подписки
GET  /api/check-all-subscriptions     # Проверка всех подписок
POST /api/channels/{id}/click         # Логирование клика
GET  /api/wheel-config                # Конфигурация рулетки
```

### Защищенные (требуют initData)

```
POST /api/user/init                   # Инициализация пользователя
POST /api/spin                        # Прокрут рулетки
POST /api/spin/notify                 # Уведомление о прокрутке
GET  /api/user/tickets                # Получить билеты
GET  /api/referral/link               # Реферальная ссылка
POST /api/referral/register           # Регистрация по ссылке
GET  /api/referral/stats              # Статистика рефералов
GET  /api/leaderboard                 # Лидерборд
POST /api/stars/exchange              # Обмен Stars
POST /api/stars/payment               # Платеж Stars
```

### Админ API (требуют авторизацию + роль admin)

```
GET    /api/v1/wow/wheel              # Список секторов
POST   /api/v1/wow/wheel              # Создать сектор
PUT    /api/v1/wow/wheel/{id}         # Обновить сектор
DELETE /api/v1/wow/wheel/{id}         # Удалить сектор
GET    /api/v1/wow/wheel/settings     # Настройки рулетки
PUT    /api/v1/wow/wheel/settings     # Обновить настройки
GET    /api/v1/wow/channels           # Список каналов
POST   /api/v1/wow/channels           # Создать канал
PUT    /api/v1/wow/channels/{id}      # Обновить канал
DELETE /api/v1/wow/channels/{id}      # Удалить канал
GET    /api/v1/wow/users              # Пользователи WOW
PUT    /api/v1/wow/users/{id}/tickets # Начислить билеты
GET    /api/v1/wow/wins               # Выигрыши
GET    /api/v1/wow/leaderboard        # Лидерборд (админ)
POST   /api/v1/wow/leaderboard/prizes # Создать приз
PUT    /api/v1/wow/leaderboard/prizes/{id} # Обновить приз
```

---

## 🗄 База данных

### Основные таблицы

**users**
- `id`, `telegram_id`, `name`, `email`, `password`
- `tickets_available` - текущее количество билетов
- `stars_balance` - баланс Stars
- `last_spin_at` - последняя прокрутка
- `last_ticket_received_at` - последнее начисление ежедневных билетов
- `tickets_depleted_at` - момент обнуления билетов (для таймера)
- `referral_popup_shown_at` - флаг показа pop-up реферала
- `invited_by` - ID пригласившего пользователя
- `total_spins`, `total_wins` - статистика

**wheel_sectors**
- `id`, `sector_number` (1-12), `prize_type`, `prize_value`
- `icon_url`, `probability_percent`, `is_active`

**wheel_settings**
- `id`, `always_empty_mode`, `ticket_restore_hours`
- `initial_tickets_count`, `daily_tickets`, `default_daily_tickets`
- `stars_per_ticket_purchase`, `stars_enabled`
- `show_gift_button`, `send_ticket_notification`
- `admin_username`, `welcome_text`, `welcome_banner_url`, `welcome_buttons`

**spins**
- `id`, `user_id`, `sector_id`, `prize_type`, `prize_value`
- `rotation`, `created_at`

**referrals**
- `id`, `inviter_id`, `invited_id`, `invited_at`

**user_tickets**
- `id`, `user_id`, `tickets_count`, `restored_at`, `source`

**star_exchanges**
- `id`, `user_id`, `stars_amount`, `tickets_received`, `status`

**channels**
- `id`, `username`, `title`, `external_url`, `priority`, `is_active`

**leaderboard_snapshots**
- `id`, `period`, `snapshot_data`, `created_at`

**leaderboard_prizes**
- `id`, `position`, `prize_amount`, `period_months`

---

## 💼 Бизнес-логика

### Начисление билетов

**Ежедневное начисление (`checkDailyTickets`):**
1. Проверяется `last_ticket_received_at`
2. Если `null` (новый пользователь) и билетов 0 → начисляется `default_daily_tickets`
3. Если прошло >= 24 часа → начисляется `daily_tickets`
4. Обновляется `last_ticket_received_at = now()`
5. Если `send_ticket_notification = true` → отправляется уведомление

**Восстановление по таймеру (`checkTicketRestore`):**
1. Проверяется `tickets_depleted_at`
2. Если прошло `ticket_restore_hours` → начисляется 1 билет
3. Если билеты > 0 → сбрасывается `tickets_depleted_at`
4. Если `send_ticket_notification = true` → отправляется уведомление

### Прокрут рулетки

1. Проверка наличия билетов
2. Проверка подписки на каналы (если требуется)
3. Выбор сектора по вероятностям
4. Списание билета
5. Сохранение в `spins`
6. Начисление приза (если применимо)
7. Отправка уведомления (если применимо)
8. Возврат результата на фронтенд

### Реферальная система

1. Пользователь получает ссылку: `https://t.me/bot?start=ref_{telegram_id}`
2. Новый пользователь переходит по ссылке
3. `TelegramWebhookController` обрабатывает `/start ref_*`
4. Создается запись в `referrals`
5. Пригласившему начисляется 1 билет
6. Отправляется уведомление пригласившему

---

## 🎨 Frontend структура

### React приложение (пользовательское)

**Роутинг:**
- `/` - главная (Index.tsx) - роутинг между StartMenu и MainWheel
- `/wheel` - рулетка (MainWheel.tsx)
- `/friends` - реферальная система (Friends.tsx)
- `/leaderboard` - лидерборд (Leaderboard.tsx)
- `/how-to-play` - инструкция (HowToPlay.tsx)

**Основные компоненты:**

**MainWheel.tsx:**
- Загрузка конфигурации рулетки
- Загрузка билетов пользователя
- Обработка прокрутки
- Отображение результата
- Управление попапами (результат, реферал, подарок)

**WheelComponent.tsx:**
- Визуализация рулетки
- Анимация прокрутки
- Обработка кликов

**SpinResultPopup.tsx:**
- Отображение результата прокрутки
- Кнопка связи с админом (для призов)
- Информация о выигрыше

**SecretGiftPopup.tsx:**
- Обмен Telegram Stars на билеты
- Интеграция с Telegram Payments API

**Хуки:**

**useTelegramWebApp.ts:**
- Инициализация Telegram WebApp
- Получение данных пользователя
- Работа с initData

**useChannelSubscription.ts:**
- Проверка подписки на каналы
- Открытие каналов в Telegram

**useUserInit.ts:**
- Инициализация пользователя на сервере
- Создание/обновление записи

### Vue приложение (админ-панель)

**Роутинг:**
- `/admin` - главная
- `/admin/wow/wheel` - редактор рулетки
- `/admin/wow/channels` - управление каналами
- `/admin/settings/stars` - настройки Stars
- `/admin/settings/bot` - настройка бота

**Основные страницы:**

**Wheel.vue:**
- Редактирование секторов
- Настройка вероятностей
- Управление настройками рулетки

**Channels.vue:**
- CRUD операции с каналами
- Настройка приоритетов

**StarsSettings.vue:**
- Настройка стоимости билетов в Stars
- Включение/выключение Stars
- Управление кнопкой "Подарок"
- Управление уведомлениями о билетах

---

## 🔌 Интеграции

### Telegram Bot API

**Сервисы:**
- `TelegramService` - валидация initData, получение данных пользователя
- `TelegramNotificationService` - отправка уведомлений
- `app/Telegram/Bot.php` - методы Bot API

**Middleware:**
- `ValidateTelegramInitData` - проверка initData на всех защищенных роутах

**Webhook:**
- `TelegramWebhookController` - обработка событий от Telegram
- Регистрация по реферальной ссылке
- Обработка команд бота

### Telegram Stars API

**Платежи:**
- Создание инвойсов через Bot API
- Обработка webhook платежей
- Обмен Stars на билеты

**Настройки:**
- `stars_per_ticket_purchase` - стоимость билетов
- `stars_enabled` - включение/выключение

---

## 📝 Правила разработки

### Обновление документации

**ВАЖНО:** При любых изменениях кода необходимо обновлять эту документацию:

1. **Изменения в структуре:**
   - Добавление новых моделей → обновить раздел "База данных"
   - Добавление новых контроллеров → обновить раздел "API Endpoints"
   - Изменение структуры папок → обновить раздел "Структура проекта"

2. **Изменения в функционале:**
   - Новая бизнес-логика → обновить раздел "Бизнес-логика"
   - Новые настройки → обновить раздел "Основные компоненты"
   - Изменения в API → обновить раздел "API Endpoints"

3. **Изменения во Frontend:**
   - Новые страницы → обновить раздел "Frontend структура"
   - Новые компоненты → обновить описание компонентов
   - Изменения в роутинге → обновить описание роутинга

4. **Новые интеграции:**
   - Добавление новых сервисов → обновить раздел "Интеграции"

### Формат обновлений

При обновлении документации:
- Указывать дату обновления
- Описывать что изменилось
- Обновлять примеры кода (если есть)
- Обновлять схемы (если есть)

### Использование документации для промптов

При составлении промптов для изменений:
1. Ссылаться на соответствующие разделы документации
2. Указывать конкретные файлы и методы
3. Описывать текущую логику перед изменениями
4. Указывать желаемый результат

**Пример промпта:**
```
Используя документацию PROJECT_DOCUMENTATION.md, раздел "Система билетов":
- Текущая логика: ежедневное начисление через checkDailyTickets()
- Задача: добавить начисление билетов за активность (например, за 10 прокруток)
- Файлы: app/Http/Controllers/Api/TicketController.php, app/Models/User.php
- Результат: новая логика начисления + обновление документации
```

---

## 🔄 Версионирование документации

**Версия 2.1** (Декабрь 2025)
- Добавлено: описание системы ежедневного начисления билетов
- Добавлено: описание настройки show_gift_button (кнопка "Подарок")
- Добавлено: описание настройки send_ticket_notification (уведомления о билетах)
- Обновлено: раздел "Система билетов" с полной логикой начисления
- Обновлено: раздел "Система уведомлений" с новыми методами

**Версия 2.0** (Декабрь 2025)
- Полная структура проекта
- Описание всех компонентов
- API endpoints
- Бизнес-логика
- Frontend структура
- Правила обновления

---

**Примечание:** Эта документация должна обновляться при каждом значительном изменении кода. Используйте её как основу для понимания проекта и составления промптов для изменений.

**См. также:**
- `DOCUMENTATION_GUIDE.md` - руководство по использованию документации
- `.cursorrules` - правила для автоматического обновления документации

