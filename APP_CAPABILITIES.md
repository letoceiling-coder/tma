# Полное описание возможностей приложения WOW Spin

## 📋 Общая информация

**WOW Spin** — это Telegram Mini App с интерактивной рулеткой для розыгрыша призов. Приложение состоит из:
- **Backend**: Laravel 12 (PHP 8.2+)
- **Frontend пользовательский**: React 18 + TypeScript + Vite
- **Frontend админ-панель**: Vue 3 + TypeScript
- **База данных**: MySQL/PostgreSQL

---

## 🎯 Основные возможности приложения

### 1. Система рулетки (Wheel System)

#### Функциональность:
- **12 секторов рулетки** с настраиваемыми призами
- **Вероятностная система** выпадения призов (настраиваемые проценты)
- **Типы призов**:
  - Денежные призы (рубли)
  - Билеты (для дополнительных прокрутов)
  - Секретные подарки
  - WOW-боксы
  - Пустые сектора (проигрыш)
- **Анимация прокрутки** на фронтенде
- **История всех прокрутов** с сохранением в БД

#### API Endpoints:
- `GET /api/wheel-config` - Получить конфигурацию рулетки (публичный)
- `POST /api/spin` - Запустить прокрут рулетки (требует initData)
- `POST /api/spin/notify` - Уведомление о завершении прокрутки

#### Модели:
- `WheelSector` - Секторы рулетки (12 штук)
- `WheelSetting` - Настройки рулетки
- `Spin` - История прокрутов
- `PrizeType` - Типы призов

#### Админ-панель:
- Редактор секторов рулетки (`/admin/wow/wheel`)
- Настройка вероятностей выпадения
- Валидация суммы вероятностей (должна быть 100%)
- Массовое обновление секторов
- Просмотр статистики выигрышей

---

### 2. Система билетов (Ticket System)

#### Функциональность:
- **Начальные билеты**: 3 билета при регистрации
- **Автоматическое восстановление**: +1 билет каждые 24 часа (если билетов < 3)
- **Покупка билетов**: Обмен Telegram Stars на билеты
- **История билетов**: Все операции с билетами сохраняются
- **Уведомления**: Автоматические напоминания о восстановлении

#### API Endpoints:
- `GET /api/user/tickets` - Получить количество билетов пользователя
- Автоматическое восстановление через cron/queue

#### Модели:
- `UserTicket` - История операций с билетами
- `User.tickets_available` - Текущее количество билетов

#### Логика:
- При прокрутке рулетки списывается 1 билет
- Если билетов нет, прокрутка недоступна
- Восстановление происходит автоматически через 24 часа после последнего использования

---

### 3. Реферальная система (Referral System)

#### Функциональность:
- **Реферальные ссылки**: Генерация уникальных ссылок для каждого пользователя
- **Регистрация по реферальной ссылке**: Автоматическое определение пригласившего
- **Бонусы за приглашения**:
  - Пригласивший получает билеты за каждого приглашенного
  - Приглашенный получает бонусные билеты при регистрации
- **Статистика рефералов**: Количество приглашенных, активных рефералов
- **Попап с реферальной ссылкой**: Показывается один раз новым пользователям

#### API Endpoints:
- `GET /api/referral/link` - Получить реферальную ссылку
- `POST /api/referral/register` - Регистрация по реферальной ссылке
- `GET /api/referral/stats` - Статистика рефералов
- `POST /api/referral/popup-shown` - Отметить, что попап показан

#### Модели:
- `Referral` - Реферальные связи
- `User.invited_by` - ID пользователя, который пригласил
- `User.referral_popup_shown_at` - Время показа попапа

#### Формат реферальной ссылки:
```
https://t.me/{bot_username}?start=ref_{telegram_id}
```

---

### 4. Лидерборд (Leaderboard)

#### Функциональность:
- **Рейтинг игроков** по количеству выигрышей
- **Призы для топ-игроков**: Настраиваемые призы за места в топе
- **Периодические награждения**: Автоматическое начисление призов в конце периода
- **Снимки лидерборда**: Сохранение состояния лидерборда для истории
- **Типы призов лидерборда**: Деньги, билеты, специальные награды

#### API Endpoints:
- `GET /api/leaderboard` - Получить текущий лидерборд (публичный)
- Админ: `GET /api/wow/leaderboard` - Детальная статистика
- Админ: `GET /api/wow/leaderboard-prizes` - Управление призами
- Админ: `POST /api/wow/leaderboard-prizes/update-period` - Обновить период

#### Модели:
- `LeaderboardSnapshot` - Снимки лидерборда
- `LeaderboardPrize` - Призы для топ-игроков
- `User.total_wins` - Общее количество выигрышей

#### Логика:
- Рейтинг формируется по количеству выигрышей (`total_wins`)
- Призы начисляются автоматически в конце периода
- Поддерживается несколько периодов одновременно

---

### 5. Обмен Telegram Stars (Star Exchange)

#### Функциональность:
- **Обмен Stars на билеты**: 50 Stars = 20 билетов
- **Двухэтапный процесс**: Инициация → Подтверждение через webhook
- **История обменов**: Все операции сохраняются
- **Webhook от Telegram**: Автоматическое подтверждение обмена

#### API Endpoints:
- `POST /api/stars/exchange` - Простой обмен (устаревший)
- `POST /api/stars/exchange/initiate` - Инициировать обмен
- `POST /api/stars/exchange/confirm` - Подтвердить обмен
- `GET /api/stars/exchange/history` - История обменов
- `POST /api/stars/exchange/webhook` - Webhook от Telegram

#### Модели:
- `StarExchange` - История обменов
- `User.stars_balance` - Баланс Stars пользователя

#### Процесс:
1. Пользователь инициирует обмен через Mini App
2. Telegram обрабатывает платеж
3. Webhook подтверждает успешный обмен
4. Билеты начисляются пользователю

---

### 6. Система подписок на каналы (Channel Subscription)

#### Функциональность:
- **Обязательные каналы**: Список каналов, на которые нужно подписаться
- **Проверка подписки**: Верификация через Telegram Bot API
- **Блокировка игры**: Нельзя играть без подписки на все каналы
- **Множественные каналы**: Поддержка нескольких обязательных каналов

#### API Endpoints:
- `GET /api/channels` - Получить список активных каналов (публичный)
- `GET /api/check-subscription/{channelUsername}` - Проверить подписку на канал
- `GET /api/check-all-subscriptions` - Проверить все подписки

#### Модели:
- `Channel` - Каналы для подписки
- Поля: `username`, `title`, `is_active`, `is_required`

#### Логика:
- Перед игрой проверяется подписка на все активные обязательные каналы
- Проверка происходит через `getChatMember` API Telegram
- Если не подписан - показывается предупреждение с кнопкой подписки

---

### 7. Система уведомлений (Notification System)

#### Функциональность:
- **Автоматические уведомления** через Telegram Bot API:
  - Выигрыш денежного приза
  - Восстановление билетов
  - Призы из лидерборда
  - Другие важные события
- **Уведомления в админ-панели**: Система внутренних уведомлений
- **История уведомлений**: Все уведомления сохраняются в БД

#### API Endpoints:
- `GET /api/notifications` - Получить уведомления (требует auth)
- `GET /api/notifications/all` - Все уведомления
- `POST /api/notifications/{id}/read` - Отметить как прочитанное
- `DELETE /api/notifications/{id}` - Удалить уведомление
- `GET /api/notifications/unread-count` - Количество непрочитанных

#### Модели:
- `Notification` - Уведомления пользователей
- `User.last_notification_sent_at` - Время последнего уведомления

#### Сервисы:
- `TelegramNotificationService` - Отправка уведомлений через Telegram
- `NotificationTool` - Вспомогательные методы для работы с уведомлениями

---

### 8. Аутентификация и авторизация

#### Функциональность:
- **Telegram Mini App аутентификация**: Валидация `initData` от Telegram
- **Sanctum токены**: Для админ-панели
- **Роли и права доступа**: Система ролей (admin, user, etc.)
- **Middleware защита**: Различные уровни доступа

#### API Endpoints:
- `POST /api/user/init` - Инициализация пользователя WOW (создание/обновление)
- `POST /api/auth/register` - Регистрация (для админ-панели)
- `POST /api/auth/login` - Вход (для админ-панели)
- `POST /api/auth/logout` - Выход
- `GET /api/auth/user` - Получить текущего пользователя

#### Middleware:
- `telegram.initdata` - Проверка Telegram initData
- `telegram.webhook` - Проверка webhook от Telegram
- `auth:sanctum` - Проверка Sanctum токена
- `admin` - Проверка прав администратора

#### Модели:
- `User` - Пользователи системы
- `Role` - Роли пользователей
- `PersonalAccessToken` - Sanctum токены

---

### 9. Админ-панель (Admin Panel)

#### Структура:
- **Vue 3** приложение в `resources/js/`
- **Маршрутизация**: Vue Router
- **API клиент**: Axios с автоматической авторизацией

#### Разделы админ-панели:

##### 9.1. WOW Рулетка (`/admin/wow/`)
- **Channels** (`/admin/wow/channels`) - Управление каналами
  - CRUD операции с каналами
  - Активация/деактивация каналов
  - Настройка обязательности подписки
  
- **Wheel** (`/admin/wow/wheel`) - Редактор рулетки
  - Редактирование секторов (12 штук)
  - Настройка вероятностей
  - Настройка типов призов
  - Валидация суммы вероятностей
  - Массовое обновление
  
- **Wins** (`/admin/wow/wins`) - История выигрышей
  - Просмотр всех выигрышей
  - Фильтрация по типам призов
  - Статистика выигрышей
  
- **Leaderboard** (`/admin/wow/leaderboard`) - Управление лидербордом
  - Просмотр текущего рейтинга
  - Настройка призов для топ-игроков
  - Обновление периодов награждения
  
- **Leaderboard Prizes** (`/admin/wow/leaderboard-prizes`) - Призы лидерборда
  - Создание/редактирование призов
  - Настройка мест и периодов
  - Массовое обновление
  
- **Prize Types** (`/admin/wow/prize-types`) - Типы призов
  - Управление типами призов
  - Настройка иконок и названий
  
- **Wow Users** (`/admin/wow/users`) - Пользователи WOW
  - Просмотр всех пользователей
  - Детальная информация о пользователе
  - Ручное добавление билетов
  - Статистика пользователя
  
- **Referrals** (`/admin/wow/referrals`) - Реферальная система
  - Просмотр реферальных связей
  - Статистика по рефералам
  
- **Statistics** (`/admin/wow/statistics`) - Общая статистика
  - Графики и аналитика
  - Метрики использования

##### 9.2. Общие разделы
- **Dashboard** (`/admin/dashboard`) - Главная страница
- **Users** (`/admin/users`) - Управление пользователями админ-панели
- **Roles** (`/admin/roles`) - Управление ролями
- **Notifications** (`/admin/notifications`) - Уведомления
- **Media** (`/admin/media`) - Медиа-библиотека
- **Bot Config** (`/admin/bot-config`) - Настройка Telegram бота
  - Настройка webhook
  - Тестирование соединения
  - Настройка кнопки меню бота

#### API Endpoints (требуют auth + admin):
- Все endpoints под `/api/wow/*` - Управление WOW функциональностью
- `GET /api/admin/menu` - Получить меню админ-панели
- `GET /api/v1/users` - Управление пользователями
- `GET /api/v1/roles` - Управление ролями
- `GET /api/settings/bot/*` - Настройки бота

---

### 10. Интеграции с мессенджерами

#### 10.1. Telegram Integration
- **Telegram Bot API**: Полная интеграция через пакет `app/Telegram/`
- **Telegram Mini App**: Валидация initData, работа с WebApp API
- **Telegram Stars API**: Обмен Stars на билеты
- **Webhook**: Обработка событий от Telegram

#### Файлы:
- `app/Telegram/Bot.php` - 40+ методов Bot API
- `app/Telegram/MiniApp.php` - Валидация Mini App
- `app/Telegram/TelegramClient.php` - HTTP клиент
- `app/Services/TelegramService.php` - Сервис для работы с Telegram
- `app/Services/TelegramNotificationService.php` - Отправка уведомлений

#### Middleware:
- `app/Http/Middleware/ValidateTelegramInitData.php` - Валидация initData

#### Команды:
- `php artisan telegram:set-webhook` - Установить webhook
- `php artisan telegram:test` - Тест соединения

#### 10.2. VK Integration
- **VK Bot API**: Интеграция через пакет `app/VK/`
- **VK Mini App**: Валидация Mini App
- **Long Poll API**: Получение обновлений

#### 10.3. Max Integration
- **Max Bot API**: Интеграция через пакет `app/Max/`
- **Max Mini App**: Валидация Mini App

---

### 11. Медиа-библиотека (Media Library)

#### Функциональность:
- **Загрузка файлов**: Изображения, документы
- **Папки**: Организация файлов в папки
- **Корзина**: Удаление с возможностью восстановления
- **Древовидная структура**: Вложенные папки

#### API Endpoints:
- `GET /api/v1/folders/tree/all` - Получить дерево папок
- `POST /api/v1/folders` - Создать папку
- `PUT /api/v1/folders/{id}` - Обновить папку
- `DELETE /api/v1/folders/{id}` - Удалить папку
- `POST /api/v1/folders/{id}/restore` - Восстановить папку
- `GET /api/v1/media` - Список медиа
- `POST /api/v1/media` - Загрузить медиа
- `DELETE /api/v1/media/{id}` - Удалить медиа
- `POST /api/v1/media/trash/empty` - Очистить корзину

#### Модели:
- `Folder` - Папки медиа-библиотеки
- `Media` - Медиа файлы

---

### 12. Система деплоя (Deploy System)

#### Функциональность:
- **Автоматический деплой**: Через API endpoint
- **Git интеграция**: Автоматический pull из репозитория
- **Composer install**: Установка зависимостей
- **Миграции**: Автоматический запуск миграций
- **Seeders**: Опциональный запуск seeders
- **Очистка кешей**: Автоматическая очистка всех кешей
- **Оптимизация**: Кеширование конфигурации, маршрутов, представлений

#### API Endpoints:
- `POST /api/deploy` - Выполнить деплой (защищен токеном)
- `POST /api/seed` - Выполнить seeders (защищен токеном)

#### Параметры:
- `branch` - Ветка для деплоя (по умолчанию текущая)
- `run_seeders` - Запустить seeders (true/false)

#### Процесс деплоя:
1. Очистка файлов разработки
2. Git pull из указанной ветки
3. Composer install
4. Очистка кеша package discovery
5. Миграции БД
6. Seeders (опционально)
7. Очистка всех кешей
8. Оптимизация приложения

---

## 📁 Структура проекта

### Backend (Laravel)

```
app/
├── Http/
│   ├── Controllers/
│   │   ├── Api/
│   │   │   ├── Admin/          # Админ API контроллеры
│   │   │   ├── v1/             # Media API
│   │   │   └── *.php           # Публичные API контроллеры
│   │   ├── Middleware/         # Middleware
│   │   └── Requests/           # Form Requests
│   ├── Resources/              # API Resources
│   └── Filters/                # Query Filters
├── Models/                     # Eloquent модели
├── Services/                   # Бизнес-логика
├── Jobs/                       # Queue jobs
├── Console/Commands/           # Artisan команды
├── Telegram/                   # Telegram интеграция
├── VK/                        # VK интеграция
├── Max/                       # Max интеграция
└── Providers/                 # Service Providers

database/
├── migrations/                # Миграции БД
└── seeders/                   # Seeders

routes/
├── api.php                    # API маршруты
└── web.php                    # Web маршруты

config/
├── telegram.php               # Конфигурация Telegram
├── vk.php                     # Конфигурация VK
├── max.php                    # Конфигурация Max
└── ...
```

### Frontend (React - пользовательское приложение)

```
frontend/
├── src/
│   ├── pages/
│   │   ├── Index.tsx          # Главная страница
│   │   ├── MainWheel.tsx      # Страница рулетки
│   │   ├── Friends.tsx        # Реферальная система
│   │   ├── Leaderboard.tsx    # Лидерборд
│   │   └── HowToPlay.tsx      # Инструкция
│   ├── components/
│   │   ├── WheelComponent.tsx # Компонент рулетки
│   │   ├── ReferralPopup.tsx  # Попап реферальной ссылки
│   │   ├── SpinResultPopup.tsx # Результат прокрутки
│   │   └── ui/                # UI компоненты (shadcn/ui)
│   ├── hooks/
│   │   ├── useTelegramWebApp.ts    # Хук для Telegram WebApp
│   │   ├── useChannelSubscription.ts # Проверка подписок
│   │   └── useUserInit.ts          # Инициализация пользователя
│   └── lib/
│       └── haptic.ts          # Тактильная обратная связь
└── package.json
```

### Frontend (Vue - админ-панель)

```
resources/js/
├── pages/
│   ├── admin/
│   │   ├── Dashboard.vue
│   │   ├── Users.vue
│   │   ├── Roles.vue
│   │   └── wow/               # WOW разделы
│   └── auth/                   # Страницы авторизации
├── components/
│   ├── admin/                 # Компоненты админ-панели
│   └── media/                 # Компоненты медиа-библиотеки
├── layouts/
│   └── AdminLayout.vue        # Основной layout
└── utils/
    └── api.js                 # API клиент
```

---

## 🗄️ База данных

### Основные таблицы:

1. **users** - Пользователи системы
   - `telegram_id` - ID в Telegram
   - `tickets_available` - Количество билетов
   - `stars_balance` - Баланс Stars
   - `total_spins` - Всего прокрутов
   - `total_wins` - Всего выигрышей
   - `invited_by` - ID пригласившего
   - `referral_popup_shown_at` - Время показа попапа

2. **wheel_sectors** - Секторы рулетки (12 штук)
   - `sector_number` - Номер сектора (1-12)
   - `prize_type` - Тип приза
   - `prize_value` - Значение приза
   - `probability_percent` - Вероятность выпадения
   - `is_active` - Активен ли сектор

3. **spins** - История прокрутов
   - `user_id` - ID пользователя
   - `sector_id` - ID сектора
   - `prize_type` - Тип приза
   - `prize_value` - Значение приза
   - `spin_time` - Время прокрутки

4. **channels** - Каналы для подписки
   - `username` - Username канала
   - `title` - Название
   - `is_active` - Активен ли
   - `is_required` - Обязателен ли

5. **referrals** - Реферальные связи
   - `inviter_id` - ID пригласившего
   - `invited_id` - ID приглашенного
   - `registered_at` - Время регистрации

6. **user_tickets** - История билетов
   - `user_id` - ID пользователя
   - `amount` - Количество (положительное/отрицательное)
   - `type` - Тип операции
   - `created_at` - Время операции

7. **star_exchanges** - Обмены Stars
   - `user_id` - ID пользователя
   - `stars_amount` - Количество Stars
   - `tickets_amount` - Количество билетов
   - `status` - Статус обмена
   - `telegram_payment_id` - ID платежа в Telegram

8. **leaderboard_snapshots** - Снимки лидерборда
   - `period_start` - Начало периода
   - `period_end` - Конец периода
   - `snapshot_data` - JSON с данными рейтинга

9. **leaderboard_prizes** - Призы лидерборда
   - `position` - Место в топе
   - `prize_type_id` - ID типа приза
   - `prize_value` - Значение приза
   - `period_start` - Начало периода
   - `period_end` - Конец периода

10. **prize_types** - Типы призов
    - `name` - Название
    - `icon_url` - URL иконки
    - `color` - Цвет

11. **notifications** - Уведомления
    - `user_id` - ID пользователя
    - `title` - Заголовок
    - `message` - Сообщение
    - `is_read` - Прочитано ли
    - `type` - Тип уведомления

12. **roles** - Роли пользователей
    - `name` - Название
    - `slug` - Уникальный идентификатор

13. **folders** - Папки медиа-библиотеки
    - `name` - Название
    - `parent_id` - ID родительской папки
    - `position` - Позиция для сортировки

14. **media** - Медиа файлы
    - `folder_id` - ID папки
    - `filename` - Имя файла
    - `path` - Путь к файлу
    - `mime_type` - MIME тип
    - `size` - Размер файла

---

## 🔐 Безопасность

### Аутентификация:
- **Telegram Mini App**: Валидация `initData` через HMAC SHA-256
- **Sanctum токены**: Для админ-панели
- **Middleware защита**: Различные уровни доступа

### Валидация:
- Все входящие данные валидируются через Form Requests
- Проверка прав доступа на уровне middleware
- Защита от CSRF атак

### Конфигурация:
- Все секретные ключи в `.env`
- SSL/TLS обязателен для production
- Защита webhook endpoints токенами

---

## 🚀 Развертывание

### Требования:
- PHP 8.2+
- Composer 2.0+
- Node.js 18.0+
- MySQL 8.0+ или PostgreSQL 13+
- SSL-сертификат (обязательно для Telegram Mini App)

### Процесс:
1. Клонирование репозитория
2. Установка зависимостей (`composer install`, `npm install`)
3. Настройка `.env` файла
4. Генерация ключа приложения (`php artisan key:generate`)
5. Миграции БД (`php artisan migrate`)
6. Seeders (`php artisan db:seed`)
7. Сборка фронтенда (`npm run build` в `frontend/` и корне)
8. Настройка веб-сервера (Apache/Nginx)
9. Настройка SSL-сертификата
10. Настройка Telegram webhook

### Автоматический деплой:
- Через API endpoint `/api/deploy` (защищен токеном)
- Поддерживает указание ветки для деплоя
- Автоматически выполняет все необходимые шаги

---

## 📝 Использование в промптах

### При создании промптов для исправлений или дополнений:

1. **Указывайте конкретный раздел**:
   - "Исправить баг в системе рулетки"
   - "Добавить новую функцию в реферальную систему"
   - "Улучшить админ-панель управления каналами"

2. **Указывайте используемые технологии**:
   - Backend: Laravel 12, PHP 8.2+
   - Frontend пользовательский: React 18, TypeScript
   - Frontend админ: Vue 3, TypeScript

3. **Указывайте связанные компоненты**:
   - Модели: "Использовать модель `User` и `Spin`"
   - API: "Использовать endpoint `/api/spin`"
   - Компоненты: "Использовать компонент `WheelComponent.tsx`"

4. **Указывайте бизнес-логику**:
   - "При прокрутке рулетки должен списываться 1 билет"
   - "Вероятности секторов должны суммироваться в 100%"
   - "Билеты восстанавливаются каждые 24 часа"

5. **Указывайте интеграции**:
   - "Использовать Telegram Bot API для отправки уведомлений"
   - "Валидация через `TelegramService::getTelegramId()`"

### Примеры хороших промптов:

**Плохой промпт:**
> "Исправить рулетку"

**Хороший промпт:**
> "Исправить баг в системе рулетки: при прокрутке не списываются билеты. Использовать модель `User` (поле `tickets_available`), контроллер `WheelController::spin()`, и убедиться, что после успешной прокрутки количество билетов уменьшается на 1 и сохраняется в БД."

**Плохой промпт:**
> "Добавить новую функцию"

**Хороший промпт:**
> "Добавить функцию отправки push-уведомления через Telegram Bot API при выигрыше денежного приза. Использовать сервис `TelegramNotificationService`, модель `Spin` для определения выигрыша, и добавить проверку типа приза `prize_type === 'money'`. Уведомление должно отправляться асинхронно через Queue Job."

---

## 🔄 Workflow разработки

### При добавлении новой функции:

1. **Backend**:
   - Создать/обновить миграцию БД (если нужно)
   - Создать/обновить модель Eloquent
   - Создать/обновить контроллер API
   - Добавить маршрут в `routes/api.php`
   - Создать Form Request для валидации (если нужно)
   - Добавить бизнес-логику в сервис (если нужно)

2. **Frontend (React)**:
   - Создать/обновить компонент
   - Добавить хук для работы с API
   - Обновить страницу
   - Добавить обработку ошибок

3. **Frontend (Vue - админ)**:
   - Создать/обновить страницу в `resources/js/pages/admin/`
   - Добавить API вызовы в `utils/api.js`
   - Обновить меню (если нужно)

4. **Тестирование**:
   - Проверить работу API через Postman/curl
   - Проверить работу фронтенда
   - Проверить интеграции (Telegram, etc.)

---

## 📚 Дополнительные ресурсы

- **README.md** - Основная документация проекта
- **INSTALLATION.md** - Подробная инструкция по установке
- **DEPLOY.md** - Документация по деплою
- **app/Telegram/README.md** - Документация Telegram интеграции
- **app/VK/README.md** - Документация VK интеграции
- **app/Max/README.md** - Документация Max интеграции

---

## 🎯 Ключевые принципы разработки

1. **Разделение ответственности**:
   - Контроллеры только обрабатывают HTTP запросы
   - Бизнес-логика в сервисах
   - Модели только работают с данными

2. **Безопасность**:
   - Всегда валидировать входящие данные
   - Использовать middleware для проверки прав
   - Не хранить секреты в коде

3. **Производительность**:
   - Использовать кеширование где возможно
   - Асинхронная обработка через Queue
   - Оптимизация запросов к БД

4. **Масштабируемость**:
   - Использовать очереди для тяжелых операций
   - Разделение на микросервисы где нужно
   - Горизонтальное масштабирование

---

**Версия документа**: 1.0  
**Последнее обновление**: Декабрь 2025  
**Приложение**: WOW Spin - Telegram Mini App с рулеткой

