# Как избежать зависаний команд - Инструкция для AI

## 🎯 Главное правило

**Для команд, которые могут длиться > 10 секунд - давать инструкции пользователю вместо прямого выполнения.**

## ✅ Что делать

### 1. Быстрые команды (< 10 сек) - выполнять:
- `php artisan route:list`
- `git status`
- `npm --version`
- Чтение файлов

### 2. Долгие команды (> 10 сек) - давать инструкции:
- `npm run build:admin` → "Выполните: npm run build:admin"
- `npm run build:react` → "Выполните: npm run build:react"  
- `php artisan migrate` → "Выполните: php artisan migrate --force"
- `git push` → "Выполните: git push origin main"

### 3. Всегда использовать флаги неинтерактивности:

**PHP Artisan:**
```bash
--no-interaction
--force
--quiet
```

**Git:**
```bash
--no-verify
--quiet
--no-edit
```

## 📋 Чек-лист перед выполнением

1. Команда выполняется < 10 секунд? → Выполнить
2. Требует интерактивного ввода? → Инструкция
3. Может зависнуть? → Инструкция
4. Имеет все флаги? → Проверить

## 🔧 Готовые решения

1. **Диагностика:** `php scripts/check-commands.php`
2. **Безопасная сборка:** `scripts\safe-build-admin.bat`
3. **Документация:** См. `TROUBLESHOOTING.md`

## 💡 Рекомендация

При необходимости выполнения долгой команды:
1. Объяснить, что нужно сделать
2. Показать точную команду
3. Дать пользователю выполнить самостоятельно

Это гарантирует отсутствие зависаний!

