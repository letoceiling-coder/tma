# WOW Spin - Полная документация по установке и управлению

## 📖 О проекте

**WOW Spin** — это мини-приложение (Telegram Mini App) с рулеткой, разработанное для Telegram. Приложение позволяет пользователям участвовать в розыгрышах призов, приглашать друзей и выигрывать денежные призы, билеты и специальные награды.

### Основные возможности:

- 🎰 **Интерактивная рулетка** с 12 секторами и настраиваемыми призами
- 🎫 **Система билетов** с автоматическим восстановлением
- 💰 **Денежные призы** с автоматическими уведомлениями
- ⭐ **Интеграция с Telegram Stars** для покупки дополнительных билетов
- 👥 **Реферальная система** с бонусами за приглашения
- 🏆 **Лидерборд** с призами для топ-игроков
- 📢 **Обязательная подписка на каналы** перед игрой
- 🔔 **Автоматические уведомления** через Telegram Bot API
- 👨‍💼 **Админ-панель** для полного управления системой

---

## 🛠 Требования к системе

### Минимальные требования:

- **PHP**: 8.1 или выше
- **Composer**: 2.0 или выше
- **Node.js**: 18.0 или выше
- **npm**: 9.0 или выше
- **MySQL**: 8.0 или выше (или PostgreSQL 13+)
- **Web-сервер**: Apache 2.4+ или Nginx 1.18+
- **SSL-сертификат** (обязательно для Telegram Mini App)

### Рекомендуемые требования:

- **PHP**: 8.2+
- **MySQL**: 8.0+
- **RAM**: минимум 2GB
- **Дисковое пространство**: минимум 1GB

---

## 📦 Установка проекта

### Шаг 1: Клонирование репозитория

```bash
git clone <repository-url> wow-spin
cd wow-spin
```

### Шаг 2: Установка зависимостей PHP

```bash
composer install
```

### Шаг 3: Установка зависимостей для админ-панели (Vue)

```bash
npm install
```

### Шаг 4: Установка зависимостей для фронтенда (React)

```bash
cd frontend
npm install
cd ..
```

### Шаг 5: Настройка окружения

Скопируйте файл `.env.example` в `.env`:

```bash
cp .env.example .env
```

Сгенерируйте ключ приложения:

```bash
php artisan key:generate
```

### Шаг 6: Настройка базы данных

Откройте файл `.env` и настройте подключение к базе данных:

```env
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=wow_spin
DB_USERNAME=your_username
DB_PASSWORD=your_password
```

### Шаг 7: Выполнение миграций

```bash
php artisan migrate
```

### Шаг 8: Заполнение начальных данных

```bash
php artisan db:seed --class=WheelSectorSeeder
php artisan db:seed --class=RoleSeeder
```

### Шаг 9: Настройка Telegram бота

В файле `.env` добавьте:

```env
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_BOT_USERNAME=your_bot_username
TELEGRAM_WEBHOOK_URL=https://your-domain.com/api/telegram/webhook
TELEGRAM_MINI_APP_URL=https://your-domain.com
```

**Как получить токен бота:**
1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен в `.env`

### Шаг 10: Настройка хранилища

```bash
php artisan storage:link
```

### Шаг 11: Сборка фронтенда

**Сборка React приложения (для пользователей):**

```bash
cd frontend
npm run build
cd ..
```

**Сборка Vue админ-панели:**

```bash
npm run build
```

### Шаг 12: Настройка веб-сервера

#### Для Apache:

Создайте файл `.htaccess` в корне проекта (если его нет) или настройте виртуальный хост:

```apache
<VirtualHost *:80>
    ServerName your-domain.com
    DocumentRoot /path/to/wow-spin/public
    
    <Directory /path/to/wow-spin/public>
        AllowOverride All
        Require all granted
    </Directory>
</VirtualHost>
```

#### Для Nginx:

```nginx
server {
    listen 80;
    server_name your-domain.com;
    root /path/to/wow-spin/public;
    index index.php;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        fastcgi_pass unix:/var/run/php/php8.2-fpm.sock;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        include fastcgi_params;
    }
}
```

### Шаг 13: Настройка SSL (обязательно для Telegram Mini App)

Telegram Mini App требует HTTPS. Настройте SSL-сертификат через Let's Encrypt:

```bash
sudo certbot --nginx -d your-domain.com
```

### Шаг 14: Настройка Telegram Mini App

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newapp`
3. Выберите вашего бота
4. Укажите название приложения
5. Загрузите иконку (512x512px)
6. Укажите URL: `https://your-domain.com`
7. Сохраните полученную ссылку на Mini App

---

## ⚙️ Настройка проекта

### Настройка прав доступа (Linux)

```bash
sudo chown -R www-data:www-data storage bootstrap/cache
sudo chmod -R 775 storage bootstrap/cache
```

### Настройка очередей (опционально)

Для обработки уведомлений в фоне:

```bash
php artisan queue:work
```

Или настройте supervisor/cron для автоматического запуска.

### Настройка расписания задач

Добавьте в crontab:

```bash
* * * * * cd /path/to/wow-spin && php artisan schedule:run >> /dev/null 2>&1
```

---

## 👨‍💼 Админ-панель

### Доступ к админ-панели

Админ-панель доступна по адресу: `https://your-domain.com/admin`

### Создание администратора

```bash
php artisan tinker
```

```php
$user = \App\Models\User::create([
    'name' => 'Admin',
    'email' => 'admin@example.com',
    'password' => bcrypt('your_password'),
]);

$adminRole = \App\Models\Role::where('slug', 'admin')->first();
$user->roles()->attach($adminRole);
```

### Разделы админ-панели

#### 1. Каналы (`/admin/wow/channels`)

**Назначение:** Управление обязательными Telegram-каналами для подписки

**Возможности:**
- ➕ Добавление новых каналов
- ✏️ Редактирование существующих каналов
- 🗑️ Удаление каналов
- 🔄 Активация/деактивация каналов
- 📊 Настройка приоритета отображения

**Как использовать:**
1. Нажмите "Добавить канал"
2. Введите username канала (без @, например: `my_channel`)
3. Введите название канала
4. Установите приоритет (чем меньше число, тем выше приоритет)
5. Сохраните

**Важно:** Пользователи не смогут войти в приложение, пока не подпишутся на все активные каналы.

---

#### 2. Рулетка (`/admin/wow/wheel`)

**Назначение:** Настройка секторов рулетки и глобальных параметров

**Возможности:**

**Настройка секторов:**
- 🎯 Выбор типа приза для каждого сектора:
  - **Пусто** — сектор без приза
  - **Деньги** — денежный приз (укажите сумму в рублях)
  - **Билет** — дополнительный билет (укажите количество)
  - **Секретный бокс** — специальный приз
- 🖼️ Загрузка иконки для сектора (через медиатеку)
- 📊 Настройка вероятности выпадения (в процентах)
- ✅ Активация/деактивация сектора

**Глобальные настройки:**
- 🔄 **Режим "Всегда пусто"** — при включении колесо всегда останавливается на пустом секторе (для тестирования)
- ⏰ **Период восстановления билета** — через сколько часов пользователь получает новый билет (от 1 до 24 часов)
- 👤 **Username администратора** — Telegram username для генерации ссылок связи (без @)

**Как настроить рулетку:**
1. Откройте раздел "Рулетка"
2. Для каждого сектора (1-12):
   - Выберите тип приза
   - Укажите значение (для денег — сумма, для билетов — количество)
   - Загрузите иконку (опционально)
   - Установите вероятность выпадения (в %)
3. **Важно:** Сумма всех вероятностей должна быть равна 100%
4. Нажмите "Сохранить все"

**Пример настройки:**
- 7 секторов "Пусто" по 9% = 63%
- 3 сектора "Деньги" (300₽, 500₽, 1000₽) по 8% = 24%
- 1 сектор "Билет" (1 билет) = 5%
- 1 сектор "Секретный бокс" = 8%
- **Итого: 100%**

---

#### 3. Пользователи WOW (`/admin/wow/users`)

**Назначение:** Управление пользователями и их данными

**Возможности:**
- 📋 Просмотр списка всех пользователей
- 🔍 Поиск по Telegram ID или username
- 📅 Фильтрация по дате регистрации
- 👤 Просмотр детальной информации о пользователе:
  - Telegram ID и username
  - Баланс билетов и звёзд
  - Количество прокрутов
  - Количество выигрышей
  - Количество приглашенных друзей
  - История последних прокрутов
- 🎫 Добавление билетов вручную
- 📊 Статистика по каждому пользователю

**Как добавить билеты пользователю:**
1. Найдите пользователя в списке
2. Нажмите на пользователя для просмотра деталей
3. В разделе "Действия" нажмите "Добавить билеты"
4. Укажите количество билетов
5. Сохраните

---

#### 4. Выигрыши (`/admin/wow/wins`)

**Назначение:** История всех выигрышей пользователей

**Возможности:**
- 📋 Таблица всех прокрутов с результатами
- 🔍 Фильтрация по:
  - Дате (от/до)
  - Пользователю (Telegram ID или username)
  - Типу приза (деньги, билет, секретный бокс, пусто)
- 📊 Отображение:
  - Дата и время выигрыша
  - Telegram ID и username пользователя
  - Тип приза
  - Сумма приза (если применимо)
  - Номер сектора

**Как использовать:**
1. Откройте раздел "Выигрыши"
2. Используйте фильтры для поиска нужных записей
3. Экспортируйте данные при необходимости (через браузер)

---

#### 5. Топ пользователей (`/admin/wow/leaderboard`)

**Назначение:** Рейтинг пользователей по различным метрикам

**Возможности:**
- 📊 Сортировка по:
  - Количеству выигрышей
  - Сумме выигрышей
  - Количеству прокрутов
  - Количеству приглашенных
- 🔄 Фильтрация по периоду:
  - День
  - Неделя
  - Всё время
- 📈 Пагинация результатов

**Как использовать:**
1. Откройте раздел "Топ пользователей"
2. Выберите период и тип сортировки
3. Просматривайте рейтинг пользователей

---

#### 6. Рефералы (`/admin/wow/referrals`)

**Назначение:** Управление реферальной системой

**Возможности:**
- 📋 Список всех реферальных связей
- 🔍 Поиск по пригласившему или приглашенному
- 📅 Фильтрация по дате
- 📊 Статистика:
  - Всего рефералов
  - Активных приглашающих
  - Топ приглашающий пользователь

---

#### 7. Статистика (`/admin/wow/statistics`)

**Назначение:** Общая аналитика системы

**Возможности:**
- 💰 Всего звёзд получено через обмены
- 🎰 Всего прокрутов выполнено
- 📊 Распределение призов по типам (с визуализацией)
- 👥 Количество активных пользователей
- 📈 Общее количество пользователей

---

#### 8. Призы лидерборда (`/admin/wow/leaderboard-prizes`)

**Назначение:** Настройка призов для лидерборда

**Возможности:**
- 🏆 Настройка призов для каждого места (1-10)
- 💰 Указание суммы приза в рублях
- 📅 Настройка периода лидерборда (в месяцах)

**Как настроить:**
1. Откройте раздел "Призы лидерборда"
2. Для каждого места укажите сумму приза
3. Установите период (например, 1 месяц)
4. Сохраните

---

## 🎮 Возможности и настройки

### Система билетов

**Параметры:**
- Максимальное количество билетов: **3**
- Один билет = одна попытка прокрута
- Автоматическое восстановление через заданный интервал (настраивается в админке)

**Настройка восстановления:**
1. Откройте раздел "Рулетка" в админке
2. В разделе "Настройки рулетки" установите "Период восстановления билета"
3. Значение от 1 до 24 часов
4. Сохраните

**Логика работы:**
- Когда у пользователя заканчиваются билеты (становится 0), фиксируется момент времени
- Через заданный интервал автоматически восстанавливается 1 билет
- Пользователь получает уведомление в Telegram

### Система призов

**Типы призов:**

1. **Деньги** (`money`)
   - Настраиваемая сумма в рублях
   - При выигрыше пользователь получает уведомление с ссылкой на администратора
   - Выплата производится вручную администратором

2. **Билет** (`ticket`)
   - Настраиваемое количество билетов
   - Автоматически добавляется к балансу пользователя
   - Если билеты были на 0, таймер восстановления сбрасывается

3. **Секретный бокс** (`secret_box`)
   - Специальный приз
   - Пользователь получает уведомление с ссылкой на администратора
   - Выдача производится вручную

4. **Пусто** (`empty`)
   - Сектор без приза
   - Пользователь не получает ничего

### Реферальная система

**Механика:**
- Каждый пользователь получает уникальную реферальную ссылку
- При регистрации нового пользователя по реферальной ссылке:
  - Создается запись в таблице `referrals`
  - Пригласившему начисляется 1 билет
  - Увеличивается счетчик приглашенных

**Настройка:**
- Бонусы за приглашения настраиваются в коде (по умолчанию: 1 билет)
- Можно изменить в `app/Http/Controllers/Api/ReferralController.php`

### Лидерборд

**Параметры:**
- Топ пользователей по количеству приглашенных
- Период: день, неделя, всё время
- Призы настраиваются в админке

**Настройка призов:**
1. Откройте раздел "Призы лидерборда"
2. Укажите сумму для каждого места (1-10)
3. Установите период (в месяцах)
4. Сохраните

### Telegram Stars

**Механика:**
- Пользователи могут обменять 50 Telegram Stars на 20 билетов
- Обмен происходит через Telegram Stars Invoice
- Автоматическое начисление билетов после подтверждения оплаты

**Настройка:**
- Курс обмена настраивается в `app/Http/Controllers/Api/StarExchangeController.php`
- По умолчанию: 50 звёзд = 20 билетов

### Уведомления

**Типы уведомлений:**
1. **Новый билет доступен** — при восстановлении билета
2. **Выигрыш приза** — при выигрыше денег, билетов или секретного бокса
3. **Напоминание о прокрутах** — если у пользователя есть неиспользованные билеты

**Настройка:**
- Уведомления отправляются автоматически через Telegram Bot API
- Требуется корректная настройка `TELEGRAM_BOT_TOKEN` в `.env`

### Ссылки на администратора

**Функция:**
- При выигрыше денежного приза или секретного бокса пользователь получает ссылку на администратора
- Ссылка содержит предустановленный текст: "Здравствуйте, я выиграл приз в WOW Spin"

**Настройка:**
1. Откройте раздел "Рулетка" в админке
2. В разделе "Настройки рулетки" укажите "Username администратора" (без @)
3. Сохраните

---

## 🔧 Дополнительные настройки

### Режим разработки

В файле `.env` установите:

```env
APP_DEBUG=true
APP_ENV=local
```

В режиме разработки:
- Отключается строгая валидация Telegram initData
- Включается подробное логирование ошибок

### Логирование

Логи находятся в `storage/logs/laravel.log`

Для просмотра в реальном времени:

```bash
tail -f storage/logs/laravel.log
```

### Оптимизация производительности

**Кэширование конфигурации:**

```bash
php artisan config:cache
php artisan route:cache
php artisan view:cache
```

**Очистка кэша:**

```bash
php artisan cache:clear
php artisan config:clear
php artisan route:clear
php artisan view:clear
```

### Резервное копирование

Рекомендуется настроить автоматическое резервное копирование базы данных:

```bash
# Пример скрипта для backup
mysqldump -u username -p database_name > backup_$(date +%Y%m%d).sql
```

---

## 🚀 Развертывание в production

### Подготовка к production

1. Установите `APP_ENV=production` в `.env`
2. Установите `APP_DEBUG=false` в `.env`
3. Выполните оптимизацию:

```bash
php artisan config:cache
php artisan route:cache
php artisan view:cache
composer install --optimize-autoloader --no-dev
```

4. Соберите фронтенд:

```bash
cd frontend
npm run build
cd ..
npm run build
```

### Безопасность

1. **Измените все пароли по умолчанию**
2. **Настройте firewall** для ограничения доступа
3. **Используйте HTTPS** (обязательно для Telegram Mini App)
4. **Регулярно обновляйте зависимости:**

```bash
composer update
npm update
```

---

## 📞 Поддержка и помощь

### Часто задаваемые вопросы

**Q: Пользователи не могут войти в приложение**
A: Проверьте:
- Настройки каналов в админке
- Токен бота в `.env`
- SSL-сертификат (должен быть валидным)

**Q: Билеты не восстанавливаются**
A: Проверьте:
- Настройку периода восстановления в админке
- Запущен ли `php artisan schedule:run` в cron
- Логи в `storage/logs/laravel.log`

**Q: Уведомления не отправляются**
A: Проверьте:
- Токен бота в `.env`
- Права бота на отправку сообщений
- Логи в `storage/logs/laravel.log`

### Полезные команды

```bash
# Очистка всех кэшей
php artisan optimize:clear

# Просмотр маршрутов
php artisan route:list

# Просмотр очередей
php artisan queue:work

# Запуск тестов
php artisan test
```

---

## 📝 Лицензия

Проект разработан для использования в коммерческих целях.

---

## 🔄 Обновление проекта

При обновлении проекта:

1. Сделайте резервную копию базы данных
2. Обновите код:

```bash
git pull origin main
```

3. Обновите зависимости:

```bash
composer install
npm install
cd frontend && npm install && cd ..
```

4. Выполните миграции:

```bash
php artisan migrate
```

5. Очистите кэш:

```bash
php artisan optimize:clear
```

6. Пересоберите фронтенд:

```bash
cd frontend
npm run build
cd ..
npm run build
```

---

**Версия документации:** 1.0  
**Дата обновления:** Декабрь 2025

