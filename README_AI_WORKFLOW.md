# Рабочий процесс для AI - Как избежать зависаний

## 🎯 Проблема

Команды часто зависают при выполнении через инструменты, особенно:
- `npm run build:admin` / `npm run build:react`
- `php artisan migrate` / `php artisan db:seed`
- `git push` / `git pull`

## ✅ Решение: Двухуровневая стратегия

### Уровень 1: Быстрые команды (< 10 сек) - ВЫПОЛНЯТЬ

**Выполнять напрямую с флагами:**
- `php artisan route:list --json`
- `git status --porcelain`
- `npm --version`
- Чтение файлов

### Уровень 2: Долгие команды (> 10 сек) - ИНСТРУКЦИИ

**Давать инструкции пользователю:**
- ❌ НЕ выполнять: `npm run build:admin`
- ✅ Вместо этого: "Выполните команду: `npm run build:admin`"

## 📋 Правила работы

### Правило 1: Всегда использовать флаги неинтерактивности

**PHP Artisan:**
```bash
--no-interaction  # Отключить интерактивность
--force           # Принудительное выполнение
--quiet           # Минимум вывода
```

**Git:**
```bash
--no-verify       # Пропустить хуки
--quiet           # Минимум вывода
--no-edit         # Не открывать редактор
```

### Правило 2: Проверять время выполнения

- < 10 секунд → Выполнить с таймаутом
- > 10 секунд → Инструкция пользователю

### Правило 3: Использовать готовые скрипты

Если есть скрипты-обёртки - использовать их:
- `scripts/safe-build-admin.bat`
- `scripts/check-commands.php`

## 🔧 Готовые инструменты

### 1. Диагностика окружения
```bash
php scripts/check-commands.php
```

### 2. Безопасная сборка
```bash
# Windows
scripts\safe-build-admin.bat

# Linux/Mac
./scripts/safe-build-admin.sh
```

### 3. Документация
- `TROUBLESHOOTING.md` - Решение проблем
- `HOW_TO_AVOID_HANGS.md` - Избежание зависаний
- `QUICK_FIX_COMMANDS.md` - Быстрое решение

## 💡 Рекомендации

1. **Для сборки проектов:** Всегда давать инструкции
2. **Для миграций:** Использовать `--force --no-interaction`
3. **Для Git операций:** Проверять состояние перед выполнением
4. **При зависании:** Прерывать и давать инструкции

## 📝 Примеры

### ✅ Правильно:
```
Для сборки админ-панели выполните:
npm run build:admin

Команда займет 1-3 минуты.
```

### ❌ Неправильно:
```bash
npm run build:admin  # Может зависнуть при выполнении
```

## 🎓 Вывод

**Главное правило:** Если команда может зависнуть или длиться долго - давать инструкции пользователю вместо прямого выполнения. Это гарантирует стабильную работу!

